# No API key is required. This release implements sample mode only.
# The server reads process environment variables; it does not auto-load .env.
# Setting another mode stops startup instead of returning sample answers.
TAIWAN_LAB_DATA_MODE=sample
